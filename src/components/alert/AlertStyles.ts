import { StyleSheet } from 'react-native';

export const alertStyles = StyleSheet.create({
    alertContainer: {
        marginTop: 10,
        alignItems: 'center',
        justifyContent: 'center',
    },
    alertText: {
        fontSize: 17,
        fontWeight: 'bold',
    },
});